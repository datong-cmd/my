# 只在继承/篡夺/复国后出现，确保无旧皇帝的临时事件链会永久占用前台槽。
minghm_review_inherited_agenda_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds" }
	desc = minghm_review_inherited_agenda_decision_desc
	is_shown = { minghm_is_ming_emperor_trigger = yes }
	is_valid_showing_failures_only = { minghm_has_inheritance_recovery_pending_trigger = yes }
	effect = { trigger_event = { id = minghm_core.0003 } }
	ai_will_do = { base = 0 }
}

# 所有长项目共享的人工中止出口；继承待复核时必须先走专用恢复流程。
minghm_abandon_active_agenda_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_destiny_goal.dds" }
	desc = minghm_abandon_active_agenda_decision_desc
	is_shown = { minghm_is_ming_emperor_trigger = yes }
	is_valid_showing_failures_only = {
		minghm_has_foreground_content_trigger = yes
		NOT = { minghm_has_inheritance_recovery_pending_trigger = yes }
	}
	effect = { trigger_event = { id = minghm_core.0004 } }
	ai_will_do = { base = 0 }
}

minghm_set_new_reign_direction_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" }
	desc = minghm_set_new_reign_direction_decision_desc
	is_shown = { minghm_is_ming_emperor_trigger = yes }
	is_valid_showing_failures_only = { minghm_has_new_reign_direction_pending_trigger = yes }
	effect = { trigger_event = { id = minghm_core.0006 } }
	ai_will_do = { base = 0 }
}
