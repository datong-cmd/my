# 28 最终需求覆盖与交付审计

## 1. 审计对象

本审计验证“可交给后续 AI 生成 Mod 代码的实现计划书”是否覆盖当前要求。它不声称尚未编写的 Mod 已通过实机测试；C01–C08、T0–T8 是编码阶段必须执行的验收门。

## 2. 要求覆盖矩阵

| 用户要求 | 权威施工证据 | 结论 |
|---|---|---|
| 与《变身大明》同时启动的兼容版新 Mod | `22A` 固定四件套、descriptor 依赖、最后加载和零公共覆盖；`27A` 产品定义 | 已覆盖 |
| 立足本体、TGP DLC 和参考模组可实现 | `22A` 底层 title/on_action/皇权接口；`23A–23C` 官位、朋党、弹劾适配；`26B` 本体 `start_war`/CB 探针 | 已覆盖，未知运行细节明确门控 |
| 每项机制写实现方法和数据 | `23–26B` 给出状态载体、字段、公式、状态机、文件落位、AI、清理和验收 | 已覆盖 |
| 只对明制度生效 | `22A` 以 `title:h_greatming` 为唯一全国载体；`27` T2/发布阻断 | 已覆盖 |
| 性能优先、适度抽象、数字对比 | `24` 八项长期数值与差值四档；`25–26` 单槽和低频结算；`27` 硬上限 | 已覆盖 |
| 不要财政、区域生产、市场 | `24` 明确弃用；`27A` 阅读优先级覆盖旧文档；`27` 发布阻断 | 已覆盖 |
| 历史唯物主义、阶级斗争与阶段变迁 | `25` 力量关系、动员对价和五档可逆阶段；`26A` 商业政治影响；`26B` 革命/反革命 | 已覆盖且非线性决定论 |
| 改革、革新、再工业化、意识形态、资本崛起 | `26/26A` 六项改革、四阶段组织革新、三种正当化模式、商业政治影响 | 已覆盖为轻量政治抽象 |
| 重大事件长期博弈、非一边倒 | `26` 六类危机与累计对抗；各动员路线有长期代价；`26B` 双方策略和妥协 | 已覆盖 |
| 人物身份尽量用参考模组 | `23B/23C/25A` 读取品级、官位、朋党、军职、宦官和锦衣卫 | 已覆盖 |
| 其他人物事件动态产生并结束释放 | `25A` 证明 CK3 不能真正释放已创建人物，因此普通群体匿名化、临时状态统一清理，仅极少长期领袖可创建 | 已覆盖并按引擎事实修正 |
| 3D 直接复用、少做 2D | `27` 零 3D 复制、MVP 新 2D 为 0、已确认 theme/background 候选和回退 | 已覆盖 |
| 方便 AI 编码和查找 | `00` 总目录；`21A` 反查索引；`27A` 唯一入口、MVP 文件和 M0–M10 | 已覆盖 |
| 分阶段推送 GitHub main | 阶段提交 `ab70451`、`b1c7a16`、`bac44cd`、`e412939`、`1df9e36`、`63ea3ca` | 已覆盖 |

## 3. 冻结后的最小系统

- 长期：八项 `h_greatming` 全国数值、历史阶段 0–4、工业组织阶段 0–4；
- 活动：一个政策槽、一个案件槽、一个危机槽，危机与政策不并行复杂结算；
- 人物：最多 8 个活动参与者、12 个候选人、4 个党派代表、2 个本模组生成重要人物；
- 更新：常态年度一次，活动季度一次；
- 展示：决议、事件、现有肖像/theme/background/图标；
- 兼容：上游身份只读，皇权只经受检适配器消费，公共文件零覆盖。

## 4. 明确未宣称完成的事项

以下属于后续代码实施，不是计划书缺口：

- 实际创建 Mod 目录和脚本；
- 在 CK3 1.19.0.6 启动并执行 C01–C08、T0–T8；
- 平衡数值经玩家测试后的调整；
- 可选革命 CB 在无地攻击者/目标 title 上的实机验证；
- 上游《变身大明》更新后的键差异适配。

这些事项已经在 `27/27A` 中具备具体入口、失败降级和验收条件，编码 AI 不需重新发明架构。

## 5. 最终交付路径

编码 AI 首先读取 [27A AI 编码交付总实施书](27A_AI编码交付总实施书.md)，按其中优先级逐份加载文档。人类策划复核从 [00 总目录与阅读路线](00_总目录与阅读路线.md) 进入。旧研究与施工版冲突时，永远以 `22–28` 为准。

